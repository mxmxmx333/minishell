/* while (1)
	{
		msh->cur_cmd_line = readline(msh->prompt);
		printf("Variable: %s\nReturned: %s\n ----------\n", msh->cur_cmd_line, variable_finder(msh, msh->cur_cmd_line));
		destroy_tokens(msh);
	} */